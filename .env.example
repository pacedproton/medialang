# Copy to .env and fill in real values. .env is git-ignored and must never be committed.
#
# Connection string for the source database used by the sql_import tool.
# Characters that are reserved in a URI (: / ? # [ ] @ , ; = and %) must be
# percent-encoded in the password field.
DATABASE_URL=postgresql://user:password@host:5432/database
